import java.util.Calendar;

public class MemoClass {
    public static void main(String[] args) {
        // 메모클래스에 저장할 정보는 name, password, text, today
        // input 클래스에서 받은 내용을 MenuList에 저장
//        Calendar today = Calendar.getInstance();
        // String[] memo = {인풋에서 입력받은 내용, today};
        // 수정 클래스에서 내용을 받으면 text, today값 재 할당 후 메뉴리스트로 전송
    }
}
